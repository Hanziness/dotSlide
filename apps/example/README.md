# dotSlide Example Template

This is a minimal example presentation built using **dotSlide**. It demonstrates:

* Creating a presentation page in `pages`
* Custom Slide templates in `components/slides`
* Styling the presentation using Tailwind CSS
* (WIP) Adding the **dotSlide Remote** for connecting to the server