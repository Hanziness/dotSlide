---
"@dotslide/framework": minor
---

Drop unused public helper functions that helped generate DOM attributes (they were only used in the old Astro codebase): `getDataTags`, `getComponentDataAttribute` and `getSelector`
  